import { CurrencyFormatPipePipe } from './currency-format-pipe.pipe';

describe('CurrencyFormatPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CurrencyFormatPipePipe();
    expect(pipe).toBeTruthy();
  });
});
